# PDF Compressor — Python Dependencies
# ======================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Also required (external tool — NOT a pip package):
#   Ghostscript: https://www.ghostscript.com/download/
#   winget install ArtifexSoftware.GhostScript

# ── Core PDF Libraries ─────────────────────────────────────────────────
pikepdf>=8.0.0          # PDF read/write and structural optimization
PyMuPDF>=1.24.0         # Fast PDF/image extraction (fitz)
Pillow>=10.0.0          # Image processing and JPEG re-encoding

# ── CLI / Utilities ────────────────────────────────────────────────────
# (No additional CLI dependencies needed — uses argparse from stdlib)

# ── Build / Distribution ───────────────────────────────────────────────
pyinstaller>=6.0.0      # Build standalone .exe

# ── Web App / API ──────────────────────────────────────────────────────
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
Flask-Login>=0.6.3
PyMySQL>=1.1.0
midtransclient>=1.3.0

# ── Security Dependencies (SECURITY FIX) ───────────────────────────────
python-dotenv>=1.0.0        # Environment variables management
Flask-Limiter>=3.5.0        # Rate limiting protection
Flask-WTF>=1.2.0            # CSRF protection
APScheduler>=3.10.0         # Background task scheduler for file cleanup




